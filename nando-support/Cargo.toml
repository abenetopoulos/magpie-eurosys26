[package]
name = "nando-support"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = "0.12.3"
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0.138"
serde_with = "3.12.0"
ulid = { version = "1.1.2", features = ["serde"] }

[features]

timing = []

object-caching = []
